{
  "name": "mantis-carp-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "format": "prettier --write .",
    "dev": "serverless offline --reloadHandler --noTimeout"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.414.0",
    "@aws-sdk/lib-dynamodb": "^3.414.0",
    "date-fns": "^2.30.0",
    "express": "^4.18.2",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "serverless": "^3.35.2",
    "serverless-offline": "^12.0.4"
  }
}
